{
    "builds": [{
        "src": "real/wsgi.py",
        "use": "@vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" ,"runtime": "python3.8"}
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "real/wsgi.py"
        }
    ]
}